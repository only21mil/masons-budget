<?xml version="1.0" encoding="utf-8"?>
<!--
  No permissions are declared, deliberately.

  This build reads sanitized fixtures and has no network, storage, camera,
  microphone or location access. Adding a permission here is a review event, not
  a convenience: the app handles family financial data and its attack surface
  should stay at zero until a feature genuinely needs one.
-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="false"
        android:icon="@android:drawable/ic_lock_lock"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.VogelVault">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:configChanges="orientation|screenSize|screenLayout|smallestScreenSize|keyboardHidden|density|uiMode"
            android:label="@string/app_name"
            android:theme="@style/Theme.VogelVault">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
</manifest>
