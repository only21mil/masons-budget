// VogelVaultCore carried only VoiceParser. Voice entry is removed from the app,
// so the target's single source and its test suite went with it. This
// placeholder keeps the SwiftPM manifest buildable; see Package.swift.
